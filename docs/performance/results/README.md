# Reference results

No physical reference-PC result has been accepted yet. Store each probe JSON
and a neighboring Markdown file containing the hardware, OS, driver, power
profile, wired-loopback method, median latency and p95 latency. CI or VM results
must be labeled diagnostic and cannot close the Phase 1 qualification gate.
