# Building from source

The root `README.md` lists supported minimum tools. Components can be developed
independently with CMake/CTest, Cargo, npm workspaces, and Python editable
installs. CI is the source of truth for supported clean-build environments.
Generated output belongs under `build/`, `target/`, `dist/`, or tool-specific
caches and must not be committed.

