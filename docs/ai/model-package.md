# Model package contract (draft)

A future model package will contain signed metadata, weights, tokenizer,
generation configuration, model card, license, checksums, task capabilities,
schema compatibility, hardware requirements, and measured benchmarks. Loading
is deny-by-default when signatures, checksums, licenses, or compatibility are
missing. No model package exists in Phase 0.

