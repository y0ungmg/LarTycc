# Desktop host

This directory will contain the native desktop shell. Phase 0 deliberately does
not lock the project to Tauri or another webview host; the choice depends on a
latency, accessibility, packaging, and IPC spike scheduled before Phase 1.

